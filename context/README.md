# Context Directory

This directory contains React context providers for managing global state.

## Files

-   **`section-context.tsx`**: This context is used to manage the background color of different sections on a page, allowing for smooth transitions between them.
