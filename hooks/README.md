# Hooks Directory

This directory contains custom React hooks used throughout the application.

## Files

-   **`use-media-query.ts`**: A hook for detecting whether a given media query is active.
-   **`use-mobile.ts`**: A hook for detecting whether the user is on a mobile device.
-   **`use-scroll-position.ts`**: A hook for tracking the user's scroll position on the page.
